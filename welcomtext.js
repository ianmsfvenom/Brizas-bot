const bienvenido = (número, nombre de grupo) => {
    devuelve `𝐎𝐥𝐚 @ $ {número}. Bienvenido al grupo $ {nombre de grupo} `
}
export.welcome = bienvenido

const bye = (número) => {
    return `Adiós @ $ {número}. Es tarde 👋`
}
exportaciones.bye = adiós
